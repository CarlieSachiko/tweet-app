<%= form_for(@tweet) do |t| %>
  <% if @tweet.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@tweet.errors.count, "error") %> prohibited this tweet from being saved:</h2>

      <ul>
      <% @tweet.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= t.label :handle %><br>
    <%= t.text_field :handle, :required => true %>
  </div>
  <div class="field">
    <%= t.label :content %><br>
    <%= t.text_field :content, :required => true%>
  </div>
  <div class="actions">
    <%= t.submit %>
  </div>
<% end %>
